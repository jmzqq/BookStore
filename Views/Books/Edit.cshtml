@using System.Globalization
@model Bookstore.Models.ViewModels.BookFormViewModel

@{
    ViewData["Title"] = "Editar Livro";

    Model.Book.Price = double.Parse(Model.Book.Price.ToString(CultureInfo.InvariantCulture));
}

<h1 class="text-primary-emphasis">@ViewData["Title"]</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <input type="hidden" asp-for="Book.Id" />
            <div class="form-group">
                <label asp-for="Book.Title" class="control-label"></label>
                <input asp-for="Book.Title" class="form-control" style="box-shadow: none" />
                <span asp-validation-for="Book.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Book.Price" class="control-label"></label>
                <div class="input-group">
                    <span class="input-group-text">R$</span>
                    <input asp-for="Book.Price" class="form-control" style="box-shadow: none" />
                </div>
                <span asp-validation-for="Book.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Book.Author" class="control-label"></label>
                <input asp-for="Book.Author" class="form-control" style="box-shadow: none"/>
                <span asp-validation-for="Book.Author" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Book.ReleaseYear" class="control-label"></label>
                <input asp-for="Book.ReleaseYear" class="form-control" style="box-shadow: none"/>
                <span asp-validation-for="Book.ReleaseYear" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SelectedGenresIds" class="control-label"></label>
                <br />
                <span class="text-muted">Segure Ctrl para selecionar mais de um item</span>
                <select multiple="multiple" asp-for="SelectedGenresIds" asp-items="Model.GenresSelect" placeholder="Selecione o Gênero Literário" class="form-select"></select>
                <span asp-validation-for="SelectedGenresIds" class="text-danger"></span>

            </div>
            <div class="form-group mt-3 mb-5">
                <input type="submit" value="Salvar" class="btn btn-success mt-2 mb-2" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="text-primary text-decoration-none">Voltar para a lista</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}